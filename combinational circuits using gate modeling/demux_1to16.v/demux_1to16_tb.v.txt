module demux_1to16_tb;
reg i;
reg a,b,c,d;
wire [15:0]s;

demux_1to16 dut(.i(i),.a(a),.b(b),.c(c),.d(d),.s(s));

integer x;

initial begin

$dumpfile("demux_1to16.vcd");
$dumpvars(0,demux_1to16_tb);

$monitor("time=%0t | i=%b,a=%b,b=%b,c=%b,d=%b | s=%b",$time,i,a,b,c,d,s);

for(x=0;x<16;x=x+1) begin
        
      i = 1;

    {a,b,c,d}=x;
        #10;
    end
$finish;
end 
endmodule
